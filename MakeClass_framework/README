plotting framework

1. open a data tree in a root session, and call MakeClass("MyClass")

2. change the line in analysis_all.C which has the base path, also set the desired output filename

3. run Loop() with $ root analysis_all.C

4. the resulting output rootflies are combined together to make plots with plot_combine.py
  change the fileprefix name to that chosen above, and name the output as desired
  run with $ python plot_combine.py

5. copy the pdf file with all plots to local machine for viewing
